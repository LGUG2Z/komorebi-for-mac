# completions

```
Generate komorebic CLI completions for the target shell

Usage: komorebic completions <SHELL>

Arguments:
  <SHELL>
          [possible values: bash, elvish, fish, powershell, zsh]

Options:
  -h, --help
          Print help

```